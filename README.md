# hypergraph-cli

## Install

Using npm:

```sh
npm install hypergraph-cli
```

Using yarn:

```sh
yarn add hypergraph-cli
```

## Usage

To run hypergraph cli, use the following command.

```sh
npx hypergraph
```
